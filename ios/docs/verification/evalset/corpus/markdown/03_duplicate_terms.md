# 术语表

## 索引

索引 = 把文档切成 chunks 并写入 SQLite（含 FTS 与向量）。

## chunk

chunk 是可检索、可引用的文本单元。容量上限按 chunk 计数。

## 检索

检索 = 在已 ready 的文档上做混合打分，再按预算挑选证据。

## 再次出现的词

离线、离线、离线：用于稀疏关键词与重复术语召回题。

本地知识库支持离线提问；离线提问依赖本机索引，不依赖云端。
