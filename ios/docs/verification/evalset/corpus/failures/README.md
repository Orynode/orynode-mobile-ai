# 失败样本（待补）

期望行为：导入阶段明确失败，**不**产生空文档或假成功。

建议放入：

- `encrypted_or_corrupt.pdf` — 加密或故意截断损坏
- （可选）极糊、OCR 后仍无有效文字的扫描页，用于确认 `emptyDocument`

说明：纯图像扫描 PDF 在启用本机 OCR 后**可能成功导入**；不再默认当作失败样本。登记到 `MANIFEST.json` 后可单独做检查清单。
